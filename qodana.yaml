version: "1.0"
linter: jetbrains/qodana-cdnet:2024.3-eap
profile:
  name: qodana.recommended
include:
  - name: CheckDependencyLicenses
  - name: VulnerableLibrariesGlobal
dotnet:
    solution: CloudFlare.Client.sln
failThreshold: 1
inspections:
  - group: ALL
    ignore:
      - "scope#file:CloudFlare.Client.Test/**"