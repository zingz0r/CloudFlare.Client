version: 2.0
jobs:
 build:
   docker:
     - image: microsoft/aspnetcore-build:2.1
   steps:
     - checkout
     - run:
         name: Restore packages
         command:
            dotnet restore ./CloudFlare.Client/CloudFlare.Client.csproj -c Release
     - run:
         name: Build App
         command:
            dotnet build /CloudFlare.Client/CloudFlare.Client.csproj -c Release